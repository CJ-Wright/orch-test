install: echo 'hi' 
build:
  pip:
    default:
      conda: pip
  python:
    default:
      conda: python
run:
  yaml:
    default:
      conda: pyyaml
    bleeding:
      pip: 'git+https://github.com/yaml/pyyaml.git#egg=yaml'
  xonsh:
    default:
      conda: xonsh
  xonda:
    default:
      pip: xonda
  pip:
    default:
      conda: pip
  rever:
    default:
      conda: rever
  pytest:
    default:
      conda: pytest==3.10.1
